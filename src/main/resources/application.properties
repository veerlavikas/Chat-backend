spring.application.name=ChatApp

# ================================
# SERVER CONFIG
# ================================
server.port=8080

# ================================
# DATABASE CONFIG (TiDB Cloud)
# ================================
# 1. Replace [HOST] with your TiDB Host (e.g., gateway01...tidbcloud.com)
# 2. Replace [DB_NAME] with the database you created (e.g., chatappdb)
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/chatappdb?sslMode=VERIFY_IDENTITY&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Kolkata

# 3. Replace with your TiDB Username (e.g., 2a4b5c.root)
spring.datasource.username=2WYUvgV18ARfopX.root

# 4. Replace with your TiDB Password
spring.datasource.password=sePmlb50mFGJksTl

# JPA / HIBERNATE
# "update" will automatically create tables in TiDB if they are missing
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ================================
# JWT CONFIG (Keep as is)
# ================================
jwt.secret=H7xvN5E9m3Q0a3Y2Zy+K4Vv9xQnPp8wDkM2FJcUoR7s=
jwt.expiration=86400000

# ================================
# MULTIPART FILE UPLOAD
# ================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# WARNING: On Render/Cloud, "uploads" folder is temporary. 
# Files will be deleted when the server restarts. 
# (For a real app, you need AWS S3 or Cloudinary, but this works for testing).
file.upload-dir=uploads

# ================================
# WEBSOCKET CONFIG
# ================================
spring.websocket.path=/ws

# ================================
# LOGGING
# ================================
logging.level.org.springframework=INFO
logging.level.com.chat.backend=DEBUG